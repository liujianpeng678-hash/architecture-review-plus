interface:
  display_name: "架构审查 Skill（Plus 版）"
  short_description: "中文评分、模块图、方案选择、本机 AI 修复与实时进度"
  default_prompt: "Use $architecture-review-plus as the default architecture audit skill. Review this project's architecture, open the Chinese dashboard and module map, and only after the user selects a plan dispatch the localhost repair flow with ordinary progress."

policy:
  allow_implicit_invocation: true
